<div class="phx-hook-subscribe-to-session info-box">
  <p>
    <%= filter_link "Unresolved", "change_resolved_state", "unresolved", @resolved_state %>
    ·
    <%= filter_link "Resolved", "change_resolved_state", "resolved", @resolved_state %>
    ·
    <%= filter_link "All comments", "change_resolved_state", "all", @resolved_state %>
  </p>
  <p>
    <%= filter_link "For me", "change_direction", "for_me", @direction %>
    ·
    <%= filter_link "By me", "change_direction", "by_me", @direction %>
    ·
    <%= filter_link "For anyone", "change_direction", "all", @direction %>
  </p>
</div>

<%= if @notifications == [] do %>
  <div class="mt-10 text-center">
    <p><i class="fad fa-star-shooting text-6xl text-yellow-500"></i></p>
    <p class="text-lg text-gray-mid pt-3">Nothing yet!</p>
  </div>
<% else %>
  <%= for notification <- @notifications do %>
    <%= live_component @socket, RemitWeb.CommentComponent,
      id: notification.id,
      notification: notification,
      comment: notification.comment,
      commit: notification.comment.commit,
      at_me?: notification.username == @username,
      resolved?: notification.resolved_at
    %>
  <% end %>
<% end %>
